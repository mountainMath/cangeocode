% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode_qc.R
\name{nar_geocode_tier_qc}
\alias{nar_geocode_tier_qc}
\title{The Quebec geocoder tier}
\usage{
nar_geocode_tier_qc(res, out, todo, con, bounds = NULL, ...)
}
\arguments{
\item{res}{The parsed components, after any authoritative override}

\item{out}{The result so far, as [nar_geocode_match()] builds it}

\item{todo}{Row indices still needing a position}

\item{con}{A NAR connection, for the storage CRS}

\item{bounds}{An `sfc` in the storage CRS, or `NULL`}

\item{...}{Passed to [qc_geocode()]}
}
\value{
`out`, with the rows the service placed filled in
}
\description{
The tier behind `geocode(method = c(..., "qc"))`. Only the rows
its predecessors left unplaced **and** that are in Quebec are sent -- the
service covers no other province.

Unlike the other two online tiers this one costs a request per *thousand*
rows rather than per row, so naming it is cheap even on a large unplaced
tail.
}
\keyword{internal}
