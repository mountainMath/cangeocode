% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode_bc.R
\name{nar_geocode_bc_fallback}
\alias{nar_geocode_bc_fallback}
\title{Send the addresses NAR could not place to the BC geocoder}
\usage{
nar_geocode_bc_fallback(res, hits, con, bounds = NULL, ...)
}
\arguments{
\item{res}{The parsed components, after any authoritative override}

\item{hits}{The result of [nar_geocode_match()]}

\item{con}{A NAR connection, for the storage CRS}

\item{bounds}{An `sfc` in the storage CRS, or `NULL`}

\item{...}{Passed to [bc_geocode()]}
}
\value{
`hits`, with the rows the service placed filled in
}
\description{
The fallback tier behind `geocode(fallback = "bc")`. Only rows
that came back `none` **and** are in British Columbia are sent -- the service
covers no other province, and asked about an Ontario address it answers with
whatever BC place shares the name.
}
\keyword{internal}
