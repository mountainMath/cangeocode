% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_variants.R
\name{nar_mun_anchor_runs}
\alias{nar_mun_anchor_runs}
\title{Trailing token runs that name a municipality}
\usage{
nar_mun_anchor_runs(toks, prov = NA_character_, min_k = 1L)
}
\arguments{
\item{toks}{A token vector, comma tokens included}

\item{prov}{A two-letter province code, or `NA`}

\item{min_k}{The shortest run worth testing}
}
\value{
An integer vector of run lengths, longest first
}
\description{
Shared by the gate and by [nar_mun_anchor_variants()] so the two
agree on what "the municipality could reach back this far" means, and so the
gate pays for the inventory lookups only down to the length it cares about.

The reach stops one token short of the last comma segment. A municipality
never spans a comma the writer put in, and taking the whole of the last
segment would only rediscover the split the comma already made.
}
\keyword{internal}
