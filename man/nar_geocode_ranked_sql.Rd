% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode.R
\name{nar_geocode_ranked_sql}
\alias{nar_geocode_ranked_sql}
\title{Return every candidate instead of one, in the order the tier ranks them}
\usage{
nar_geocode_ranked_sql(cand, rank)
}
\arguments{
\item{cand}{SQL producing the candidate set, with a `row_id`}

\item{rank}{The tier's `ORDER BY` expression}
}
\value{
A single SQL string
}
\description{
The other reading of the same candidate set
[nar_geocode_best_sql()] collapses. No aggregation and no `QUALIFY`: the
rank becomes a column rather than a filter, so row 1 of each `row_id` is the
row the collapsing query would have kept.
}
\keyword{internal}
