% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode_nrcan.R
\name{nar_geocode_tier_nrcan}
\alias{nar_geocode_tier_nrcan}
\title{The NRCan geolocator tier}
\usage{
nar_geocode_tier_nrcan(res, out, todo, con, bounds = NULL, ...)
}
\arguments{
\item{res}{The parsed components, after any authoritative override}

\item{out}{The result so far, as [nar_geocode_match()] builds it}

\item{todo}{Row indices still needing a position}

\item{con}{A NAR connection, for the storage CRS and the gazetteer}

\item{bounds}{An `sfc` in the storage CRS, or `NULL`}

\item{...}{Passed to [nrcan_geocode()]}
}
\value{
`out`, with the rows the service placed filled in
}
\description{
The tier behind `geocode(method = c(..., "nrcan"))`. Unlike the
BC tier there is no province restriction to apply -- the service is national.
It is offered only the rows its predecessors left unplaced, and it must sit
last: its median answer is an order of magnitude coarser than a NAR building
point and its tail is far longer.
}
\keyword{internal}
