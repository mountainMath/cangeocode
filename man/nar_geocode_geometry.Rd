% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode.R
\name{nar_geocode_geometry}
\alias{nar_geocode_geometry}
\title{Attach coordinates to a geocoding result}
\usage{
nar_geocode_geometry(out, x, y, con, crs = 4326, geometry = FALSE)
}
\arguments{
\item{out}{The result data frame}

\item{x, y}{Coordinates in the storage CRS, `NA` where nothing matched}

\item{con}{A NAR connection}

\item{crs}{Target CRS}

\item{geometry}{Whether to return an `sf` object}
}
\value{
`out` with coordinates attached
}
\description{
Storage-CRS coordinates in, either `lon`/`lat` columns or an
`sf` object out. The reprojection is done in `sf` rather than in DuckDB
because these are freshly computed coordinates rather than a stored geometry
column, and because it leaves the axis-order handling to `sf`, which always
means lon/lat -- the `always_xy` trap that [collect_nar()] has to work around
does not arise. The storage CRS is still read from the database rather than
assumed.
}
\keyword{internal}
