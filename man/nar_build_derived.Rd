% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nar.R
\name{nar_build_derived}
\alias{nar_build_derived}
\title{Build the gazetteer tables normalize_address() resolves against}
\usage{
nar_build_derived(con)
}
\arguments{
\item{con}{A writable DuckDB connection}
}
\value{
The connection, invisibly
}
\description{
These are aggregates over the whole `Addresses` table, so on an
append they are rebuilt rather than added to -- a street that gained
addresses needs its counts and civic-number range recomputed, not extended.
They cost a few grouped scans, which is small beside the import that
preceded them.
}
\keyword{internal}
