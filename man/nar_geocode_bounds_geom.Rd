% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode.R
\name{nar_geocode_bounds_geom}
\alias{nar_geocode_bounds_geom}
\title{Resolve `within` to a geometry in the storage CRS}
\usage{
nar_geocode_bounds_geom(within, crs, con)
}
\arguments{
\item{within}{An `sf`/`sfc`/`sfg`, an `st_bbox`, or a length-4 numeric}

\item{crs}{CRS to interpret a bare numeric or an untagged geometry in}

\item{con}{A NAR connection, for the storage CRS}
}
\value{
An `sfc` in the storage CRS, or `NULL`
}
\description{
Split out from the SQL so the same restriction can be enforced
twice: pushed into the NAR query as a predicate, and applied in R to points
that came from somewhere else -- the BC fallback, which is a separate service
and cannot be given this package's SQL.
}
\keyword{internal}
