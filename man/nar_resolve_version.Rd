% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nar.R
\name{nar_resolve_version}
\alias{nar_resolve_version}
\title{Resolve a requested version, preferring the cache over the network}
\usage{
nar_resolve_version(version, cache_path, refresh = FALSE)
}
\arguments{
\item{version}{Requested version, or `"latest"`}

\item{cache_path}{Directory holding the cached databases}

\item{refresh}{Whether the database is being rebuilt, which always needs the
download URL and so always needs the network}
}
\value{
A version key
}
\description{
`nar_connection()` used to resolve every request against the
StatCan publication page before looking at the cache, which made an already
downloaded multi-gigabyte database unusable offline. A version key that names
a cached database is now answered locally, and `"latest"` falls back to the
newest cached database when StatCan cannot be reached.

Resolving still needs the network when there is a genuine question to answer:
which release is currently latest, or which key a label like `"May 2024"`
corresponds to.
}
\keyword{internal}
