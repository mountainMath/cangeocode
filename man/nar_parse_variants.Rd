% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_variants.R
\name{nar_parse_variants}
\alias{nar_parse_variants}
\title{Every reading of one address string worth arbitrating between}
\usage{
nar_parse_variants(s, lang = "en", prov = NA_character_)
}
\arguments{
\item{s}{A single normalized string, province and postal code already removed}

\item{lang}{`"en"` or `"fr"`, deciding the canonical forms}

\item{prov}{A two-letter province code, or `NA`. Only the numbered-road
step consults it, and only for the entries that are province-specific.}
}
\value{
A data frame of one row per candidate, in priority order, carrying
the columns [nar_parse_one()] returns plus `strategy` and `.cand`
}
\description{
Candidate 1 is the ordinary left-to-right parse, unchanged.
The rest come from [nar_mun_anchor_variants()], which finds the municipality
first and hands the parser the remainder.

Identical readings are collapsed, which is the common case: a
comma-delimited string already puts the municipality in a segment of its own,
so anchoring rediscovers the same split the comma made and there is nothing
to arbitrate.
}
\keyword{internal}
