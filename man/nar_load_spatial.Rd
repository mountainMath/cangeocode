% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo_helpers.R
\name{nar_load_spatial}
\alias{nar_load_spatial}
\title{Load the DuckDB spatial extension on a connection}
\usage{
nar_load_spatial(con)
}
\arguments{
\item{con}{A DuckDB connection}
}
\value{
The connection, invisibly
}
\description{
Loads the extension, installing it first if it is not present.
This deliberately uses DuckDB's own `LOAD spatial` rather than
`duckspatial::ddbs_load()`: the latter creates *persistent* helper macros,
which fails outright on the read-only connections this package hands out.
All spatial SQL the package needs is native to the extension, plus the
TEMP macros registered by [nar_register_spatial()].
}
\keyword{internal}
