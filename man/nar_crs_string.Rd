% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo_helpers.R
\name{nar_crs_string}
\alias{nar_crs_string}
\title{Render a CRS as a string DuckDB's spatial extension accepts}
\usage{
nar_crs_string(crs)
}
\arguments{
\item{crs}{An EPSG code, an authority string, or an `sf` crs object}
}
\value{
A length-1 character CRS identifier
}
\description{
DuckDB wants an authority string such as `"EPSG:4326"`; the
bare number `4326` that `sf` and the rest of this package take happily is a
binder error there. Everything user-supplied is funnelled through here so a
numeric CRS works the same way in [collect_nar()] as it does in
[reverse_geocode()].
}
\keyword{internal}
