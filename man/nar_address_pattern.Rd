% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_pattern.R
\name{nar_address_pattern}
\alias{nar_address_pattern}
\title{Sort parsed addresses into buckets by the shape they parsed as}
\usage{
nar_address_pattern(res, traits, marks)
}
\arguments{
\item{res}{A tibble from [nar_parse_rules()]}

\item{traits}{A character vector of comma-joined parse traits, one per row}

\item{marks}{A character vector from [nar_delivery_marks()]}
}
\value{
A factor with the levels [nar_address_patterns()] gives
}
\description{
Assigns each row exactly one pattern, testing in the order
[nar_address_patterns()] lists. The order is what makes the buckets useful:
the regional and structural quirks are checked before the ordinary forms, so
`grid` and `french_street` describe the addresses that are actually unusual
rather than being swamped by the `civic_street` majority they overlap with.
A row therefore reports the most specific thing true of it, and only that.
}
\keyword{internal}
