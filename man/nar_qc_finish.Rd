% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode_qc.R
\name{nar_qc_finish}
\alias{nar_qc_finish}
\title{Turn lon/lat columns into whatever the caller asked for}
\usage{
nar_qc_finish(out, crs = 4326, geometry = FALSE)
}
\arguments{
\item{out}{A data frame carrying `lon` and `lat`}

\item{crs}{CRS to return}

\item{geometry}{Whether to return an `sf` object}
}
\value{
`out`, reprojected, with an `sf` geometry column or `lon`/`lat`
}
\description{
Shared by [qc_geocode()] and [qc_reverse_geocode()] so the two
cannot drift. The service is asked for EPSG:4326 and `sf` means lon/lat by
that name, so there is no axis-order question to get wrong before the
transform.
}
\keyword{internal}
