% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_gazetteer.R
\name{nar_gazetteer_pass}
\alias{nar_gazetteer_pass}
\title{Score one gazetteer against every candidate reading, and adopt the winner}
\usage{
nar_gazetteer_pass(
  res,
  cand,
  con,
  eligible,
  sql_fn,
  source,
  threshold = 0.85,
  name_threshold = 0.9,
  prepare = NULL
)
}
\arguments{
\item{res}{The rows being resolved, carrying `.row`}

\item{cand}{Every candidate reading, carrying `.row`, `.cand` and `.probe`}

\item{con}{An open NAR connection}

\item{eligible}{Logical over `cand`: which readings this pass may probe}

\item{sql_fn}{A function of `(probe_table, name_threshold)` returning SQL}

\item{source}{The `parse_source` value a match from this pass earns}

\item{threshold}{Minimum combined score for a match to be accepted}

\item{name_threshold}{Minimum name similarity, passed to `sql_fn`}

\item{prepare}{Optional function of `con`, run once there is work to do}
}
\value{
`res`, with matched rows replaced by their canonical values
}
\description{
The machinery both passes share: build the probe, score it
database-side, take one winner per input row, and write the canonical values
back. Only the eligible rows, the query and the `parse_source` label differ,
which is why they are arguments rather than two copies of this.
}
\keyword{internal}
