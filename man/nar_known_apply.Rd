% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/known.R
\name{nar_known_apply}
\alias{nar_known_apply}
\title{Overwrite parsed components with the ones the caller asserted}
\usage{
nar_known_apply(res, k, rows = seq_len(nrow(res)))
}
\arguments{
\item{res}{A parse, one row per address}

\item{k}{The recycled `known` frame, or `NULL`}

\item{rows}{Which row of `k` each row of `res` belongs to}
}
\value{
`res`, with the asserted components written in
}
\description{
Applied twice in [normalize_address()]: before the gazetteer, so
it restricts on what was asserted rather than on what the string happened to
yield, and after it, so a substitution the gazetteer would otherwise make
cannot overwrite the caller.
}
\keyword{internal}
