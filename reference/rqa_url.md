# The RQA bulk download

One URL, in one place, so the note and the code cannot drift apart. 778
MB, extracting to a ~3 GB \`RQA.csv\` plus a small
\`Odonymes_renvois.csv\` this package does not currently read.

## Usage

``` r
rqa_url()
```

## Value

A URL string
