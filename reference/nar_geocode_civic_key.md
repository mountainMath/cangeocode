# The civic-number half of an exact match

Shared by the query that answers and the query that enumerates, for the
same reason the rank is: two spellings of "which addresses count as this
one" would be two different searches.

## Usage

``` r
nar_geocode_civic_key()
```

## Value

A SQL fragment, appended to the street key
