# Split a token vector on comma tokens

Split a token vector on comma tokens

## Usage

``` r
nar_split_commas(toks)
```

## Arguments

- toks:

  A character vector of tokens

## Value

A list of non-empty token vectors
