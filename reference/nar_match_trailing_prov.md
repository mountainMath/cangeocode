# Strip a trailing province name from a normalized string

Strip a trailing province name from a normalized string

## Usage

``` r
nar_match_trailing_prov(s)
```

## Arguments

- s:

  A single normalized address string

## Value

A list with \`prov\` and the remaining \`rest\`
