# The BC Address Geocoder endpoint

Kept in one place so a test can point it somewhere else and so the URL
is not repeated across the request builders.

## Usage

``` r
nar_bc_url()
```

## Value

A single URL
