# Coverage of a cached NAR database, without holding the connection open

Coverage of a cached NAR database, without holding the connection open

## Usage

``` r
nar_cached_coverage(nar_path)
```

## Arguments

- nar_path:

  Path to the \`\<version\>.duckdb\` file

## Value

\`"ALL"\` or a character vector of abbreviations
