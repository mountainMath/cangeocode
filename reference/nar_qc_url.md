# The Quebec address geocoder endpoint

Kept in one place so a test can point it somewhere else and so the URL
is not repeated across the request builders. Published as a CC-BY
resource of the \`Adresses Québec\` dataset on Données Québec.

## Usage

``` r
nar_qc_url()
```

## Value

A single URL
