# The NRCan geolocator endpoint

Kept in one place so a test can point it somewhere else. The older
\`geogratis.gc.ca/services/geolocation\` host redirects here.

## Usage

``` r
nar_nrcan_url()
```

## Value

A single URL
