# Everything the NAR gazetteer pass needs in place before it runs

Everything the NAR gazetteer pass needs in place before it runs

## Usage

``` r
nar_gazetteer_prepare(con)
```

## Arguments

- con:

  A NAR connection

## Value

Invisibly \`TRUE\`
