# Whether a NAR table carries plain coordinate columns

Whether a NAR table carries plain coordinate columns

## Usage

``` r
nar_has_xy(tbl)
```

## Arguments

- tbl:

  A lazy table

## Value

\`TRUE\` if both \`x\` and \`y\` columns are present
