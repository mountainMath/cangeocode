# The value that marks a database as holding the whole country

Kept as a constant rather than spelled out at each comparison, because
it is written into \`nar_metadata\` and read back by \[nar_coverage()\];
the two have to agree exactly.

## Usage

``` r
nar_all_provinces()
```

## Value

A character scalar
