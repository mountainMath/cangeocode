# Parse address strings with the lexicon rules alone

Parse address strings with the lexicon rules alone

## Usage

``` r
nar_parse_rules(x, prov = NULL)
```

## Arguments

- x:

  A character vector of address strings

- prov:

  Optional character vector of province codes, already recycled

## Value

A tibble, one row per input
