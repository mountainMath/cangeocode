# Read one metadata value with a fallback

Read one metadata value with a fallback

## Usage

``` r
nar_meta_value(con, key, default)
```

## Arguments

- con:

  A DuckDB connection

- key:

  Metadata key

- default:

  Value to use when the key is absent

## Value

A character scalar
